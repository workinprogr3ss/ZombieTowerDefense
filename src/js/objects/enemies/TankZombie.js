import Enemy from "./EnemyObject.js";

// Export Tank class
export default class TankZombie extends Enemy {
    constructor(scene, x, y, texture) {
        super(scene, x, y, texture, 200, 1);
    }

    // Specialized methods

}